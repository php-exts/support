{
  "name": "ext/support",
  "description": "PHP Support Library",
  "type": "library",
  "license": "MIT",
  "homepage": "https://github.com/ext/support",
  "keywords": [
    "php",
    "support",
    "library"
  ],
  "authors": [
    {
      "name": "Cloudflying",
      "email": "oss@live.hk",
      "homepage": "https://github.com/imxieke",
      "role": "Developer"
    }
  ],
  "require": {
    "php": ">=8.2",
    "ext/exception": "^0.0.4"
  },
  "require-dev": {
    "mockery/mockery": "^1.6",
    "phpunit/phpunit": "^10.2.4",
    "phpstan/phpstan": "^1.10.18",
    "symfony/var-dumper": "^6.3.5",
    "vimeo/psalm": "^5.10.0"
  },
  "autoload": {
    "psr-4": {
      "Zeus\\": "src/"
    },
    "files": []
  },
  "scripts": {
    "test": "phpunit --colors",
    "psalm": "psalm --show-info=true --no-cache",
    "psalm-fix": "psalm --no-cache --alter --issues=MissingReturnType,MissingParamType",
	"phpstan": "vendor/bin/phpstan analyze src/ --memory-limit 512M --level 9"
  },
  "scripts-descriptions": {
    "test": "Run all tests."
  },
  "config": {
    "preferred-install": "dist",
    "sort-packages": true,
    "process-timeout": 300,
    "optimize-autoloader": true
  }
}
